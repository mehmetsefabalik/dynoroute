/*
 dynoroute will provide a solution for creating dynamically routed applications.
 To specify, web servers will be able to change its routing on runtime.
*/

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
